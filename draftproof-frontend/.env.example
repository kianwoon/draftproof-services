VITE_API_BASE_URL=http://localhost:8000
VITE_SITE_URL=http://localhost:3000

# Note: the Turnstile SITE key is NOT a frontend build var. The widget fetches it
# at runtime from GET /api/feedback/config — set TURNSTILE_SITE_KEY on the API.
