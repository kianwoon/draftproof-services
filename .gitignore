# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Build
dist/
build/

# Env
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Uploads
uploads/

# DB
*.db
*.sqlite3

# NonCommercial calibration data (PERSUADE) — internal only, never redistribute
poc/calibration/authorship_cases_local/
poc/calibration/_persuade_raw/

# Brainstorming visual-companion artifacts
.superpowers/

# V7 re-tune generated manifest (contains local_only SCoCESLE rows; never commit)
poc/calibration/retune/corpus/

# V7 re-tune paid-recalibration staging (candidate artifacts only; never committed baselines)
poc/calibration/retune/staging/

# V7 re-tune persistent deep-scan cache (local scored-numbers artifact; no essay text stored)
poc/calibration/retune/cache/

# V7 fused-gate run progress log (resumable-run artifact from v7_fused_gate_run.py; regenerated per run)
poc/calibration/v7_fused_gate_progress.jsonl

# V7 §12 validation corpus (SCoCESLE-derived variants + manifest — no-redistribution license; never commit)
poc/calibration/v12_validation/corpus/
